# Why I like Hapi more than Express

These snippets require Node 4.X+ to run.

```
$ node --version
```

To start Hapi:

```
$ cd hapi
$ npm install
$ npm start
```

To start Express:

```
$ cd express
$ npm install
$ npm start
```
